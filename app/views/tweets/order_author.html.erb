<p id="notice"><%= notice %></p>

<h1>Listing Tweets based on Author Name</h1>

<table class = table table-bordered>
  <thead>
    <tr>
      <th>Author</th>
      <th>Body</th>
      <th>Appropriateness</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @tweets.each do |tweet| %>
      <tr>
        <td><%= tweet.author %></td>
        <td><%= tweet.body %></td>
        <td><%= tweet.approp_score %></td>
        <td><%= link_to 'Show ', tweet %></td>
        <td><%= link_to 'Edit ', edit_tweet_path(tweet) %></td>
        <td><%= link_to 'Destroy', tweet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<a href="/tweets/new" class = index>Create a New Tweet</a>
<a href="/tweets/:tweet_id/order_author" class = index>Sort by Author name</a>
<a href="/tweets/:tweet_id/order_approp" class = index>Sort by Appropriateness</a>
<a href="/tweets/:tweet_id/first" class = index>Check out your first tweet</a>